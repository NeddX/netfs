# Auto-generated by ccpinit 

# Fetch all the source and header files and the then add them automatically
file(GLOB_RECURSE NETFS_SERVER_SOURCES "src/*.c")
file(GLOB_RECURSE NETFS_SERVER_HEADERS "src/*.h")

add_executable(nfserver ${NETFS_SERVER_SOURCES} ${NETFS_SERVER_HEADERS})
