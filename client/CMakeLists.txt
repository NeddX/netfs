# Auto-generated by ccpinit 

# Fetch all the source and header files and the then add them automatically
file(GLOB_RECURSE NETFS_CLIENT_SOURCES "src/*.c")
file(GLOB_RECURSE NETFS_CLIENT_HEADERS "src/*.h")

add_executable(nfclient ${NETFS_CLIENT_SOURCES} ${NETFS_CLIENT_HEADERS})
